{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "node ../scripts/prepare-build.cjs && pnpm build",
  "outputDirectory": "dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
